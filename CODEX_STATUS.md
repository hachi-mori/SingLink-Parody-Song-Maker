# Codex作業状況

最終更新: 2026-07-16

## 現在の状態

指定3例文の動的替え歌スコア生成について調査と仕様固定が完了し、実装開始前のチェックポイントです。

- 現在のブランチ: `codex/onomatopoeia-dynamic-score`
- 基点コミット: `ecddea5 長時間運用向けの管理ファイルを追加`
- ブロッカー: なし

## 完了

- 承認済み6時間プロンプト、管理ファイル、Git状態を照合
- 旧C++/JavaScript実装を読み取り専用で比較
- server/direct両合成経路と固定音符範囲を調査
- 基礎スコア、完成形JSON、テスト期待値を調査
- 指定3例文の正解値を確定
  - 48音符
  - 1129 frame
  - difference 11
  - phraseRanges `[0,13) / [13,28) / [28,48)`
- 末尾「に」バグの原因を、既存notes配列を切り詰めない上書き処理と特定

## 現在の作業

- 実装役へ、共有スコア生成、3例文、アセット、両合成経路、再生同期の実装を委任する

## 最後に成功した確認

- 調査役による現行Webテスト: 3ファイル、12件成功
- 調査役3体はファイルを変更していない
- 外部旧リポジトリは読み取り専用を維持

## 現在のGit状態に関する注意

- `web/shared/tests/csv.test.ts` は以前の短時間試験による未追跡ファイルである。
- 今回の変更へ混ぜず、変更、削除、ステージ、コミットしない。
- 今回のコミットではすべて対象パスを明示してステージする。

## 次に行うこと

1. 計画・状態ファイルだけを最初のチェックポイントとしてコミットする
2. 実装役を1体だけ起動する
3. 実装後に親が差分と確認結果を検証する
4. 別の読み取り専用レビュー役を起動する

## 保留事項

- 全100例文、形態素解析、他曲対応は今回の非目標であり、`BACKLOG.md` へ記録する
- VOICEVOX実合成はローカル起動状態に依存する
- 歌声は約12秒、伴奏は16秒のため、末尾約4秒は伴奏のみとなる見込み

## 再開後の最初の作業

`git status` とこのファイルを照合し、実装が未開始なら `CODEX_PLAN.md` の実装方針に従って実装役を1体だけ起動する。

## 実装チェックポイント 1（2026-07-16）

- 旧 `score-service.js` を元に共有の純粋関数 `web/shared/src/memorizationScore.ts` を追加した。
- 旧基礎スコアJSONと伴奏WAVを元ファイル名のままWebアセットへコピーし、SHA-256がコピー元と一致することを確認した。
- 生成結果が48音符、1129 frame、difference 11、範囲 `[0,13) / [13,28) / [28,48)` になる単体テストを追加した。
- 対象テスト `shared/tests/memorizationScore.test.ts` は4件成功した。
- 次は指定3例文を固定順でUIへ統合し、server/direct両合成経路を共有生成関数へ切り替える。

## 実装チェックポイント 2（2026-07-16）

- 指定3例文を固定順で出題し、表示文と歌唱用読みを保持するよう統合した。
- オノマトペ曲だけ旧基礎Score JSONと旧伴奏WAVを参照し、server/direct両経路を共有生成・合成計画へ切り替えた。
- 旧固定範囲を削除し、正誤 `[true,false,true]` から話者 `[3003,3076,3003]` を選ぶテストを追加した。歌唱クエリ話者6000は維持している。
- 結果画面の歌声と伴奏を同時ワンショット再生へ変更し、伴奏がある場合は伴奏終了時に再生状態を終了する。
- 対象テスト5件、全17テスト、型チェック、Webビルド、`git diff --check`、旧固定範囲検索に成功した。
- 任意の `tsx -e` リポジトリスモーク確認は呼び出し形式とtop-level awaitのため2回失敗したので、運用上限に従い再試行を停止した。製品コードの失敗ではなく、型チェック・ビルドは成功している。
- VOICEVOX実接続確認は未実施。次は親による差分確認と読み取り専用レビューを行う。
- コミット、push、Pull Requestは実施していない。既存未追跡 `web/shared/tests/csv.test.ts` は変更していない。

## レビュー修正チェックポイント 1（2026-07-16）

- 共有合成計画が第2・第3区間へ追加していた各2 frame休符を削除した。
- 各セグメントのnotesが元スコアのrange sliceと完全一致し、各378 / 375 / 376 frame、合計1129 frameになる回帰テストへ修正した。
- server/static双方でオノマトペの `trackName` を「幸せなら手をたたこう」に上書きした。
- 対象テスト5件、全17テスト、型チェック、Webビルドに成功した。

## レビュー修正チェックポイント 2（2026-07-16）

- VOICEVOX 0.25.1実確認で、区間先頭休符なしでは `/frame_synthesis` が400となることを受け、第2・第3区間へAPI用2 frame休符を復元した。
- 合成計画へ `leadingPaddingFrames` を追加し、server/direct両方で合成済みWAVの対応PCMを連結前に除去するよう変更した。
- WAVのsampleRate/blockAlignをfmtチャンクから読み、累積padding frame位置との差分で削除サンプル数を丸める共有純粋関数を追加した。
- 生成Score自体は48音符・1129 frameのまま、各区間の実効frameは378 / 375 / 376、合計1129であることをテストした。
- WAVトリム後のRIFF/dataサイズとPCM先頭位置を固定するテストを追加した。
- 対象7テスト、全19テスト、型チェック、Webビルドに成功した。実VOICEVOX再確認は親が行う。
