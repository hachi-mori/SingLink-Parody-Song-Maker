# Codex作業状況

最終更新: 2026-07-16

## 現在の状態

指定3例文の動的替え歌スコア生成について調査と仕様固定が完了し、実装開始前のチェックポイントです。

- 現在のブランチ: `codex/onomatopoeia-dynamic-score`
- 基点コミット: `ecddea5 長時間運用向けの管理ファイルを追加`
- ブロッカー: なし

## 完了

- 承認済み6時間プロンプト、管理ファイル、Git状態を照合
- 旧C++/JavaScript実装を読み取り専用で比較
- server/direct両合成経路と固定音符範囲を調査
- 基礎スコア、完成形JSON、テスト期待値を調査
- 指定3例文の正解値を確定
  - 48音符
  - 1129 frame
  - difference 11
  - phraseRanges `[0,13) / [13,28) / [28,48)`
- 末尾「に」バグの原因を、既存notes配列を切り詰めない上書き処理と特定

## 現在の作業

- 実装役へ、共有スコア生成、3例文、アセット、両合成経路、再生同期の実装を委任する

## 最後に成功した確認

- 調査役による現行Webテスト: 3ファイル、12件成功
- 調査役3体はファイルを変更していない
- 外部旧リポジトリは読み取り専用を維持

## 現在のGit状態に関する注意

- `web/shared/tests/csv.test.ts` は以前の短時間試験による未追跡ファイルである。
- 今回の変更へ混ぜず、変更、削除、ステージ、コミットしない。
- 今回のコミットではすべて対象パスを明示してステージする。

## 次に行うこと

1. 計画・状態ファイルだけを最初のチェックポイントとしてコミットする
2. 実装役を1体だけ起動する
3. 実装後に親が差分と確認結果を検証する
4. 別の読み取り専用レビュー役を起動する

## 保留事項

- 全100例文、形態素解析、他曲対応は今回の非目標であり、`BACKLOG.md` へ記録する
- VOICEVOX実合成はローカル起動状態に依存する
- 歌声は約12秒、伴奏は16秒のため、末尾約4秒は伴奏のみとなる見込み

## 再開後の最初の作業

`git status` とこのファイルを照合し、実装が未開始なら `CODEX_PLAN.md` の実装方針に従って実装役を1体だけ起動する。
