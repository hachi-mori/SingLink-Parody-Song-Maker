# ブロッカー

ユーザー判断、外部権限、秘密情報、破壊的変更などが必要で、安全に自律判断できない項目を記録します。

## 現在のブロッカー

なし。

## 解消済み・非ブロッキング記録

- 区間先頭休符を削除した際、VOICEVOX 0.25.1の`/frame_synthesis`が400を返した。API用2 frame休符を復元し、合成後WAVから対応PCMを共有処理で除去することで、合成成功と実効1129 frameを両立して解消した。
- direct経路の補助スモークコマンドは引用符問題で2回、TypeScript import制約で1回失敗した。ローカル静的UIは`.github.io`条件外であり、server/directの共有コード、単体テスト、型チェック、ビルド、レビューは完了しているためコア完了を止めない。GitHub Pages実環境での確認は`BL-005`として人間確認へ引き継ぐ。

## 記録形式

### BLOCKER-XXX: タイトル

- 発見日時:
- 関係する目標:
- 停止している作業:
- 判断が必要な理由:
- 安全に実施済みの確認:
- 選択肢と影響:
- ユーザーに確認したいこと:
- 独立して継続できる作業:

## 運用ルール

- 複数の妥当な仕様があり、既存資料から決められない場合は推測で実装しません。
- 外部公開、push、PR作成、本番操作、秘密情報へのアクセス、依存更新が必要な場合は停止します。
- ブロッカーがあっても、`CODEX_PLAN.md` の範囲内で独立して進められる安全な作業は継続できます。
- 解消時は、判断内容と根拠を追記してから作業を再開します。
