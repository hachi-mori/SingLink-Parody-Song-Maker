# AGENTS instructions

このリポジトリに関する応答は原則としてすべて日本語で行ってください。
コミットメッセージも日本語中心で書いてください。

## 開発環境・ビルド

- 本プロジェクトは C++ / Siv3D / Visual Studio を前提とします。
- ビルド・実行確認は Visual Studio から手動で行うため、ビルド実行確認は行わないでください。
- 特別な理由がない限り、既存のソリューション構成・プロジェクト設定（Debug/Release、x64 など）を変更しないでください。

## C++ / Siv3D 実装ルール

- Siv3D 特有の記法や設計方針（`U""` 文字列、`String`、`Array`、`SceneManager` 運用など）は、まず既存実装を参照して合わせてください。
- 独自実装を追加する前に、同種の処理が既存コード（`Main.cpp`、`Common.hpp`、`Scene/*.cpp`）にないか確認し、命名・責務分割・描画/更新の流れを踏襲してください。
- 文字コードは既存ファイルに合わせて扱い、日本語テキストを壊さないように注意してください。
- アセットパス（`App/Texture`、`App/Inst`、`App/Score` など）は既存の相対パス構成を維持してください。

## 変更時の必須手順

コードやドキュメントを変更した場合は次の手順を守ってください：
1. `git status` で変更内容を確認し、`git commit` で保存すること。
2. テストやビルド手順が用意されている場合は実行を試み、その結果をPRの説明に含めること。
3. Pull Request を作成する際は `make_pr` を利用し、日本語で変更点の要約とテスト結果を記述してください。

より深い階層に別の `AGENTS.md` が存在する場合は、その指示を優先してください。

## 長時間作業の運用

- 作業開始時は `CODEX_PLAN.md`、`CODEX_STATUS.md`、`git status`、直近のコミットを確認してください。
- `CODEX_PLAN.md` の目標、変更可能範囲、完了条件に「未設定」が残っている間は、調査と計画更新までに留め、実装を開始しないでください。
- 書き込みを行う実装役は、同じ変更範囲につき原則1体とします。調査役とレビュー役は読み取り専用にしてください。
- 計画外の問題は修正せず `BACKLOG.md` に記録し、仕様判断や安全上の判断が必要な項目は `BLOCKERS.md` に記録してください。
- 意味のあるチェックポイント、確認コマンド完了時、中断前に `CODEX_STATUS.md` を更新してください。
- 同一コマンドの再試行は最大2回、レビューと修正は最大3巡とします。同じ問題が解消しない場合は原因と次の判断事項を記録して停止してください。
- レビュー指摘は、重大なものは必ず修正、中程度は今回の目標に関係する場合のみ修正、軽微なものは原則 `BACKLOG.md` への記録に留めます。
- `git push`、Pull Request作成、本番デプロイ、秘密情報へのアクセス、依存関係の更新、大量削除は、ユーザーが今回の作業で明示的に許可しない限り行わないでください。
- 中断と再開には `docs/codex-long-run-prompts.md` のプロンプトを使用してください。
