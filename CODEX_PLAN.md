# 長時間作業計画

最終更新: 2026-07-16

## 目標

Web版へ旧「Memorization song creator」の替え歌スコア生成ロジックを移植し、指定された3つのオノマトペ例文を「幸せなら手をたたこう」の譜面へ1文1フレーズで割り当てる。Node.jsサーバー経由とブラウザ直接VOICEVOX接続の両方で同じ共有ロジックを使い、不正解行だけ「なみだめ」話者へ切り替える。

## 仕様の正本

`AGENTS.md` のリポジトリルールを常に守る。そのうえで今回の優先順位は次のとおり。

1. 承認済みの6時間作業プロンプト
2. この計画で確定した受け入れ条件
3. 旧アプリの実装済みコード
4. 添付の完成形JSON
5. 添付PDFの初期案

## 対象例文

1. `しとしとと雨がふっている。` / `しとしととあめがふっている`
2. `ぶんぶんとハチが飛ぶ音がする。` / `ぶんぶんとはちがとぶおとがする`
3. `わくわくしながらプレゼントの箱を開ける。` / `わくわくしながらぷれぜんとのはこをあける`

表示と歌唱はこの3例文だけとし、既存の末尾固定歌詞2行は含めない。不正解でも正しい例文を歌い、該当フレーズの話者だけを変更する。

## 変更してよい範囲

- `web/shared/`
- `web/server/`
- `web/client/`
- `web/assets/score/`
- `web/assets/inst/`
- Web版の関連テスト
- `CODEX_PLAN.md`
- `CODEX_STATUS.md`
- `BACKLOG.md`
- `BLOCKERS.md`
- 必要最小限のWeb版ドキュメント

## 変更してはいけない範囲

- `ずんだもんアイドルPJ/` 以下
- Visual Studioのソリューション・プロジェクト設定
- 外部の `Memorization song creator` リポジトリ
- 認証情報、本番環境、データベース、デプロイ設定
- 依存パッケージと `package-lock.json`
- 今回の目的に関係しない機能とアセット
- 既存の未追跡 `web/shared/tests/csv.test.ts`

## 実装方針

- 旧Web版 `score-service.js` を直接の移植元とする。
- 共有の純粋関数を `web/shared/src/memorizationScore.ts` に置く。
- 生成結果は `score`、`phraseRanges`、`difference` を返す。
- `phraseRanges` は生成スコア全体の半開区間とし、先頭休符込みで `[0,13)`、`[13,28)`、`[28,48)` を期待値とする。
- 出力音符は生成済みフレーズから新しい配列を構築し、元譜面の未使用音符を残さない。
- 旧基礎スコアJSONと伴奏WAVは元のファイル名を保ってWebアセットへコピーする。
- 現在の `オノマトペ.vvproj` は曲一覧との互換用に残し、合成時だけ基礎Score JSONを使用する。
- 指定3例文の歌唱用読みを既存の正誤タスクへ任意メタデータとして持たせる。
- server/direct双方の固定音符範囲と話者選択を共有の合成計画関数へ集約する。
- 歌声約12秒と伴奏16秒が独立ループして同期崩れを起こさないよう、結果画面はワンショット再生へ変更する。

## 作業項目

- [x] 現状と旧実装を読み取り専用で調査する
- [x] 受け入れ基準と変更範囲を確定する
- [x] 本番基礎Scoreアセットを読み込むゴールデン相当テストを追加する
- [x] 純粋なスコア生成関数と単体テストを追加する
- [x] 指定3例文の表示文・歌唱用読みを統合する
- [x] 旧伴奏と基礎スコアを曲情報へ関連付ける
- [x] server/direct両経路を共有スコア・合成計画へ切り替える
- [x] 結果画面の再生同期をワンショットへ変更する
- [x] 関連テスト、全テスト、型チェック、Webビルドを実行する
- [x] VOICEVOX 0.25.1でserver経路の実合成を確認する
- [x] 読み取り専用レビューを行い、指摘を修正する
- [x] `CODEX_STATUS.md` とバックログを最終更新する
- [x] 意味のある単位でコミットする

## 受け入れ基準

- 生成結果は48音符、合計1129 frame、`difference === 11` である。
- 各フレーズは13・15・20音符、378・375・376 frameである。
- `phraseRanges` は `[0,13)`、`[13,28)`、`[28,48)` である。
- 最終音符は歌詞「る」、frame 47、key 67で、不要な「に」がない。
- 歌詞列は添付完成形JSONの先頭48音符と一致する。
- 分割前後で使用対象フレーズのframe単位の音高系列が一致する。
- 入力スコアを破壊せず、同じ入力から決定的な結果を返す。
- 正誤 `[true,false,true]` の話者は `[3003,3076,3003]` となる。
- 正誤を変更しても生成歌詞は正しい例文のままである。
- server/direct両経路に旧固定範囲が残らない。
- 結果表示は指定3例文だけで、固定歌詞2行を含まない。
- 旧スコアと伴奏をWeb版から読み込める。
- `npm run test`、`npm run typecheck`、`npm run build`、`git diff --check` が成功する。
- C++版、依存定義、既存未追跡CSVテストに変更がない。
- レビューで重大・中程度の未解決指摘がない。

## エージェント構成と上限

- 親エージェント: 範囲確定、統合、コミット、最終確認
- 調査役: 旧アルゴリズム、Web統合、テスト設計の3体（完了、読み取り専用）
- 実装役: 1体のみ
- レビュー役: 実装役と別の読み取り専用エージェント
- 同時稼働: 最大4体
- 同一失敗の再試行: 最大2回
- レビュー修正: 最大3巡

## 確認コマンド

```powershell
cd web
npx vitest run --config vitest.config.ts shared/tests/memorizationScore.test.ts shared/tests/wav.test.ts
npm run test
npm run typecheck
npm run build

cd ..
rg -n "\[0,\s*16\]|\[16,\s*31\]|\[31,\s*47\]" web/server/src web/client/src
git diff --check
git status --short
```

## 非目標とバックログ候補

- 全100例文への対応
- 実行時形態素解析
- 一般用途の文章分割
- 他の曲への適用
- 旧アルゴリズムに残るが指定3例文へ影響しない分岐の整理
- C++ / Siv3D版への移植
- push、Pull Request、デプロイ

## 停止条件

- 受け入れ基準をすべて満たした
- 同じ問題を2回試しても解決できない
- ユーザー判断なしでは仕様を確定できない
- 依存更新、外部書き込み、秘密情報、破壊的変更が必要になった
- 6時間上限に達し、安全なチェックポイントを残した

停止時は理由、最後に成功した確認、Git状態、再開後の最初の作業を `CODEX_STATUS.md` に記録する。
