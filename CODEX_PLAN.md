# OpenAI Build Week 固定問題デモ改修計画

最終更新: 2026-07-19

## 目標

OpenAI Build Week版を、開始直後にリザルトを再生するデモから、通常版と同様に問題へ回答してから歌を再生するデモへ変更する。問題と再生音声は固定し、VOICEVOXがなくても動作するようにする。

## 変更可能範囲

- `web/client` のBuild Weekデモ分岐、問題画面、画面遷移、リザルト再生
- `web/shared` の固定デモデータ定義（必要な場合）
- `web/assets` の既存固定音声アセットと、その配信設定
- 関連テスト
- `CODEX_PLAN.md`、`CODEX_STATUS.md`
- 計画外の問題を記録する `BACKLOG.md` / `BLOCKERS.md`

## 変更しない範囲

- C++ / Siv3D版
- 通常版のVOICEVOX生成フローと既存のランダム問題の挙動
- 既存のソリューション構成、依存関係、公開設定
- `git push`、Pull Request作成、本番デプロイ

## 完了条件

- Build Weekデモ開始時に、通常版と同じ問題回答フローが始まる
- デモの問題は毎回同じ内容・順序になる
- 回答内容にかかわらず、最終的に全問正解時として用意した固定音声が再生される
- デモ実行中にVOICEVOX APIを必要としない
- 通常版のランダム問題・動的音声生成に影響しない
- 関連する自動テスト、型検査、差分検査を実行する（Visual Studioビルドは行わない）
- 変更を日本語コミットで保存する

## 作業手順

- [x] 開始時状態、既存ワークツリー、直近コミットを確認する
- [x] Build Weekデモの分岐と既存固定アセットを調査する
- [x] 固定問題から通常の回答画面を通って固定音声へ進むよう分離実装する
- [x] 関連テストと静的検査を実行する
- [x] 独立レビューを行い、必要な指摘を反映する
- [x] `CODEX_STATUS.md` を更新し、コミットする
