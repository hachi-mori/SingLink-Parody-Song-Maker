# 長時間作業計画

このファイルは長時間作業ごとに更新します。「未設定」が残っている間は実装を開始しません。

## 目標

未設定（開始前に、達成する成果を1～3文で記載する）

## 仕様の正本

`AGENTS.md` のリポジトリルールを常に守ります。そのうえで、今回の仕様資料を優先順位の高い順に記載します。

1. 未設定（例: `docs/app-spec.md` の対象節）
2. このファイルで確定した作業範囲と完了条件

競合や複数の解釈がある場合は推測で実装せず、`BLOCKERS.md` に記録します。

## 変更してよい範囲

- 未設定（ディレクトリ、機能、ファイルを具体的に記載する）

## 変更してはいけない範囲

- 目標に関係しないC++版またはWeb版のコード
- Visual Studioのソリューション構成とプロジェクト設定
- 認証情報、秘密情報、本番環境、外部公開設定
- データベース構造
- 依存パッケージとロックファイル
- アセットの一括移動、削除、変換

## 作業項目

- [ ] 現状と既存仕様を調査する
- [ ] 変更範囲と確認方法を確定する
- [ ] 失敗を再現するテストまたは確認手順を用意する
- [ ] 必要最小限の実装を行う
- [ ] 関連テストを実行する
- [ ] 全体確認を実行する
- [ ] 実装役とは別の役がレビューする
- [ ] 必要なレビュー修正を行う（最大3巡）
- [ ] `CODEX_STATUS.md` を最終更新する
- [ ] 意味のある単位でコミットする

## 確認コマンド

対象に合わせ、不要なコマンドは実行せず理由を `CODEX_STATUS.md` に記録します。

```powershell
# Web版を変更した場合（web/ で実行）
npm ci
npm run typecheck
npm run test
npm run build

# 共通確認（リポジトリルートで実行）
git diff --check
git status --short
```

C++ / Siv3D版のVisual Studioビルドと実行確認は人間が手動で行うため、Codexは実行しません。

## 完了条件

- [ ] 未設定（機能または修正の具体的な受け入れ条件）
- [ ] 正常系と必要な異常系が確認できる
- [ ] 既存の関連テストが成功する
- [ ] 型チェックなど対象範囲の静的確認が成功する
- [ ] レビューで重大・中程度の未解決指摘がない
- [ ] 変更範囲外の差分がない
- [ ] `CODEX_STATUS.md` に最終状態と確認結果が記録されている

## エージェント構成と上限

- 親エージェント: 全体管理、範囲選定、統合、最終確認
- 調査役: 読み取り専用。必要な観点ごとに最大3体
- 実装役: 同じ変更範囲につき1体
- レビュー役: 読み取り専用。最大2体
- 同時稼働: 最大4体
- 同一失敗の再試行: 最大2回
- レビュー修正: 最大3巡

## チェックポイント

各チェックポイントで `git status` と差分を確認し、`CODEX_STATUS.md` を更新します。

1. 調査と範囲確定後
2. 再現テストまたは確認手順の作成後
3. 対象実装と関連テスト成功後
4. 全体確認成功後
5. レビュー修正完了後

## 停止条件

- 完了条件をすべて満たした
- 同じ問題を規定回数試しても解決できない
- ユーザー判断なしでは仕様を確定できない
- 秘密情報、外部公開、破壊的変更、依存更新が必要になった
- 安全かつ計画内で進められる作業が残っていない

停止時は、理由、最後に成功した確認、Git状態、再開後の最初の作業を `CODEX_STATUS.md` に記録します。
