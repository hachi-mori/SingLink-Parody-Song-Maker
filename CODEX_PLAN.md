# OpenAI Build Week 提出版 実装計画

最終更新: 2026-07-19

## 目標

最新の `origin/openai-build-week`（基準コミット `67e63f8` を含む）を基点に、英語話者が主要操作を理解でき、335件すべての自作オノマトペ教材に英語意味・英訳例文を表示できるWeb版を作る。結果画面では既存ScoreとVOICEVOXの実フレーム情報から歌唱位置を求め、音声に同期するカラオケ風歌詞表示を行う。

## 変更可能範囲

- `web/client/`
- `web/shared/`
- `web/assets/dict/`
- `web/scripts/`
- `web/docs/`
- Web版の関連テスト
- `CODEX_PLAN.md`、`CODEX_STATUS.md`、`BACKLOG.md`、`BLOCKERS.md`

## 変更禁止範囲

- C++ / Siv3D版
- Visual Studioのソリューション・プロジェクト設定
- `web/assets/score/` と `web/assets/inst/` の既存5曲アセット
- 出版社教材由来データの復活
- 依存関係と `package-lock.json`（必要性が判明しユーザー許可を得た場合を除く）
- push、PR、デプロイ

## 設計

### 英語教材データ

- `cards_english_data.json` を日本語教材とは分離し、`schemaVersion` と `records` を持たせる。
- 各レコードは `onomatopoeia`、`meaning`、`exampleJapanese`、`exampleEnglish`、`reviewStatus`、任意の `reviewNote` を持つ。
- `onomatopoeia` で結合し、`exampleJapanese` が日本語教材の例文と完全一致することをテストする。
- 欠損時は日本語例文を維持し、英語欄に `Translation unavailable` を表示する。ただし335件の欠損・空欄・重複・対応ずれはテスト失敗とする。
- `reviewStatus: needsReview` を監査候補として抽出できるスクリプトを用意する。

### 英語UI

- React Contextによる `en` / `ja` 切替を追加する。
- 初回は `navigator.languages` を参照し、日本語環境のみ `ja`、それ以外は `en` とする。
- 選択は `localStorage` に保存し、`html[lang]` とページタイトルも更新する。
- オノマトペ、例文、回答、曲名は日本語学習対象として維持し、操作・説明・エラー・状態を翻訳する。
- 日本語文字が焼き込まれた操作画像は英語時に可視英語ラベルを重ね、説明画像は英語HTML説明を併設する。

### カラオケ同期

- `createMemorizationScore` が返す `phraseRanges` と各ノートの `frame_length` を使う。
- VOICEVOX歌唱フレームレート `93.75 fps` でフレーズ開始・終了秒を導出する。
- `AudioContext.currentTime` と開始オフセットから再生位置を更新し、未歌唱・歌唱中・歌唱済みを区別する。
- 歌唱中行はフレーズ内進捗を左から右の塗りで示し、英語字幕を日本語の下へ表示する。
- 一時停止・再開は同じオフセットから、最初からは0秒から、終了時は全行歌唱済み状態にする。
- `prefers-reduced-motion` では遷移アニメーションを抑制する。

## 作業手順

1. 基準ブランチ、335件、5曲、ランダム3問を確認する。
2. 音声・Score・frame・phraseRanges・再生処理を読み取り専用で調査する。
3. 英語データスキーマと同期方式を確定する。
4. 英語データ読込・監査テストを追加する。
5. 言語設定と主要画面の英語UIを実装する。
6. カラオケ時刻導出と結果画面の同期表示を実装する。
7. テスト・型チェック・Webビルド・差分検査を実行する。
8. 1280x720、820x1180、390x844でブラウザ確認する。
9. VOICEVOXが利用可能なら代表ケースを実音声確認する。
10. 読み取り専用レビューを行い、重大・今回範囲内の中程度指摘を修正する。
11. 状態を更新し、日本語コミットを作成する。

## 完了条件

- 英語話者がタイトル、曲選択、クイズ、正誤、生成、結果、再生、履歴、VOICEVOX案内を理解できる。
- 335件すべてに英語意味と英訳例文があり、日本語例文との対応テストが成功する。
- 監査候補を人間が確認しやすい形で列挙できる。
- 結果画面で3行の日本語歌詞と英語字幕が表示され、実フレーム由来時刻で状態が進む。
- 再生、一時停止、再開、最初から、終了で表示状態が破綻しない。
- 1280x720、820x1180、390x844で主要画面に大きな崩れがない。
- 自作335件、5曲、ランダム3問、動的スコア、両VOICEVOX経路、話者切替、伴奏同期、IndexedDB履歴を維持する。
- C++ / Siv3D版と既存5曲アセットに差分がない。
- `npm.cmd run test`、`npm.cmd run typecheck`、必要なWebビルド、`git diff --check` が成功する。
- 日本語コミットで保存され、push、PR、デプロイを行っていない。

## エージェント運用

- 親エージェント: 調査統合、設計、全実装、修正、最終確認、コミット。
- 調査エージェント: 音声同期情報と英語UI文字列の読み取り専用調査。
- レビューエージェント: 実装後の読み取り専用レビュー。
- 書き込み担当は親エージェント1体のみとする。

## 上限・停止条件

- 同一コマンドの再試行は最大2回、レビューと修正は最大3巡。
- 既存335教材の損失、C++版変更、依存更新、秘密情報アクセスが必要になった場合は停止する。
- 仕様判断が必要で安全に決められない場合は `BLOCKERS.md` に記録して停止する。
